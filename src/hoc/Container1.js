// import React from "react";
//higher order components (hoc) now put elemnts in <container>...</container> / can use of array too [.....] and react-fragment
const Container1 = (props) => props.children;  //put nested element in it(elemnets of products)

export default Container1;